进行训练后，产生的结果存储于runs/train/{taskname}，内容如下：
* 名字前有 ! 是重要的，有 ? 是可能重要的

!F1 curve, F1曲线，F1值是一个类似于mAP0.5的指标
hyp.yaml, 记录超参数文件路径
?labels_co... , 不太懂
opt.yaml, 记录训练配置，应该与恢复训练有关
!P/PR/R curve , PR相关曲线，P是准确率（模型识别的正确标签占模型识别的所有标签比例），R是召回率（模型正确识别到的标签占实际所有标签的比例）
!results, 记录每一轮epoch的训练与验证属性，只有数值指标为
    (train)  任务名称  当前轮次/总轮次  占用显存  box损失值  cls损失值  dfl损失值  总损失值  标签数  图片尺寸
    (val)  任务名称  图片数  标签数  P  R  mAP0.5  mAP0.5:0.95

    box/cls/dfl损失值分别指
    box 预测边界框（bounding box）与真实边界框之间的位置偏差
    Cls 预测类别与真实类别之间的分类误差
    DFL（Distribution Focal Loss）是 YOLOv8 引入的一种损失函数，用于优化边界框的回归精度。它基于分布建模，假设边界框的坐标不是单一值，而是一个分布

    总损失值由以上损失值加权获得，具体各所占权重可在hyp配置文件中查找
!其余图片皆可以有作用，但我还没了解
!weights文件夹，存储训练完成后的最终模型/最优模型/各任务最优模型，pt文件格式。

0.6378    0.6426    0.6412    0.4234         1     0.693    0.9774
